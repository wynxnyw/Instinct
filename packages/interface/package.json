{
  "name": "@instinct-prj/interface",
  "version": "0.3.2",
  "main": "dist/index.js",
  "types": "dist/index",
  "files": ["dist"],
  "scripts": {
    "build": "rm -rf dist &&  && rm -rf tsconfig.build.tsbuildinfo && tsc -b tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "ts-node": "9.0.0",
    "tsconfig-paths": "3.9.0",
    "typescript": "~4.0.5"
  }
}
