{
  "name": "@instinct-prj/interface",
  "private": true,
  "version": "1.0.1",
  "main": "build/",
  "scripts": {
    "build": "tsc -p .",
    "deploy:unsafe": "npm run build && cp README.md build/README.md && cp package.json build/package.json && cp package-lock.json build/package-lock.json && npm publish build",
    "deploy": "npm run test && npm run deploy:unsafe",
    "test": "tsc"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.10",
    "ts-jest": "^26.4.1",
    "ts-node": "9.0.0",
    "tsconfig-paths": "3.9.0",
    "tslint": "^6.1.2",
    "typescript": "~4.0.3"
  }
}
