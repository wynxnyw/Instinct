{
  "name": "@instinct-prj/interface-rp",
  "version": "1.2.1",
  "main": "dist/index.js",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@instinct-prj/interface": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "rimraf": "^3.0.2",
    "ts-node": "9.0.0",
    "tsconfig-paths": "3.9.0",
    "typescript": "^4.0.5"
  }
}
